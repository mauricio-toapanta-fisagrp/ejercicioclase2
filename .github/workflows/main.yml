name: CI-DockerBuild

on: 
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build-and-deploy:
    uses: mauricio-toapanta-fisagrp/devops-pipelines/.github/workflows/docker-workflow.yml@main
    with:
      docker_image_name: hola-mundo-clase-86-10
    secrets: inherit